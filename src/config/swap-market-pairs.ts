import { tokenProvider } from '@providers/tokens'

export default {
    PCR: [
      tokenProvider.DAI,
      tokenProvider.USDC,
      tokenProvider.USDT,
      tokenProvider.BUSD,
      tokenProvider.wBTC,
      tokenProvider.wETH,
    ],
}



